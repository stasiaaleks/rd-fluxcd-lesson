apiVersion: apps/v1
kind: Deployment
metadata:
  name: course-app
spec:
  template:
    spec:
      containers:
        - name: course-app
          resources:
            requests:
              memory: "256Mi"
              cpu: "200m"
            limits:
              memory: "512Mi"
              cpu: "500m"
