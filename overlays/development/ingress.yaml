apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: course-app
spec:
  rules:
    - host: course-app.dev.local
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: course-app-service
                port:
                  number: 8080
